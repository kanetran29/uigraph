// gauntlet g09: centralized route-constants map consumed via navigate(ROUTES.x)
/** Central map of named route paths used across the app. */
export const ROUTES = {
  account: '/account',
  help: '/help',
} as const
