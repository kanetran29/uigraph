{
  "name": "uigraph-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "eslint": "^10.5.0",
    "playwright-core": "^1.60.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "^8.61.0",
    "vitest": "catalog:"
  }
}
